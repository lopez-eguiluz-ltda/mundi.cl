---
import type { IconType } from "react-icons";

interface Props {
  name: string;
  link: string;
  icon: IconType;
}

const { icon: Icon, link, name } = Astro.props;
---

<a
  href={link}
  class="bg-white text-accent-normal border-[1px] border-accent-normal hover:bg-accent-normal hover:text-white p-2 rounded-full duration-300 ease-in-out"
  target="_blank"
  rel="noopener noreferrer"
  aria-label={`Visítanos en ${name}`}
>
  <Icon />
</a>
